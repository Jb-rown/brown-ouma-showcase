# Newsletter server settings
NEWSLETTER_PORT=4000
OWNER_EMAIL=you@example.com
# SMTP settings (optional) - configure to send real emails when someone subscribes
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@example.com
